# Cloud Run / RunPod API image dependencies.
#
# Installed and actually exercised in a real venv (Python 3.11.9, win_amd64)
# against `api.main`'s real, full import chain — not just a static read.
# That exercise is what caught scikit-learn's absence below: it isn't a
# direct api/ dependency, but training/squat_correctness.py's
# `from sklearn.metrics import ...` is reached at import time via
# application.workers -> inference.squat_ai_mvp -> training.squat_correctness
# (see .dockerignore's comment on why training/ ships at all). Still not a
# real `pip freeze` — no Docker/GPU environment has run the actual container
# build. If you regenerate this from `pip freeze` inside a real build, keep
# scikit-learn in it; a static dependency read alone will not surface it
# (matplotlib and scipy are also reachable in the container, but arrive as
# mediapipe's own transitive dependencies — nothing to pin for those here).
#
# torch is installed separately in the Dockerfile(s) — CPU-only wheel index
# for Cloud Run (no GPU), CUDA wheel index for RunPod. Do not add torch here.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
httpx==0.28.1
pydantic==2.10.4

numpy==1.26.4
pandas==2.2.3
opencv-python-headless==4.10.0.84
mediapipe==0.10.18
Pillow==11.0.0
scikit-learn==1.9.0
